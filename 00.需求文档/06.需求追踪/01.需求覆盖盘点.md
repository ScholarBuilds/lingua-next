---
title: 需求覆盖盘点
date: 2026-08-17
updated: 2026-09-06
tags:
  - 需求追踪
categories: 需求文档
description: 功能模块逐条 FR/BR/AC 对照代码的完成度审计；模块 13/14 与练习引擎为 2026-08-19 落地后复核；模块 20–22 为 2026-09-02 按 CR-010 目标口径盘点。
---

# 需求覆盖盘点

## 2026-09-06 当前增量核验

当前变更以 [CR-025](../07.需求变更/25.CR-025-数据可靠性与每日学习恢复.md) 为准。以下三类证据独立记录，代码存在或测试通过不等同于真实页面和发布环境验收。后文保留原日期的历史盘点，不作为当前缺陷清单；Jarvis 与桌面接管已按 CR-016 退场，历史模块说明不代表仍需建设。

| 范围 | 已实现 | 自动化验证 | 真实运行验收 |
| --- | --- | --- | --- |
| 编辑与学习可靠性 | 按画布保存、提交回执和版本、阶段统一、设置串行同步、素材全批预检 | 延迟、冲突、并发、存储失败与第六项引用用例 | 画布、设置与素材完整故障演练仍待进行 |
| 任务与桌面恢复 | 活跃去重、派发意图、历史游标、备份迁移、进程预算、导航及更新边界 | 队列、升级回退、签名篡改及来源校验 | 本地 Redis/ARQ 执行、任务历史分页与定位通过；打包恢复窗口和真实更新源未验收 |
| 今日安排 | 汇总正式与本机断点、置顶、当日暂放、失效说明 | 时区、跨午夜、偏好恢复、历史置顶内容删除 | 桌面与窄窗口、深浅主题、刷新恢复、键盘关闭与练习返回通过 |
| 结构与性能 | SQL 分页与汇总、懒加载与页面隔离、公共 HTTP、工坊职责拆分 | 1,603/10,000 词、传输错误、路由失败、画布行为回归 | 页面模块下载失败后仍可导航并重新加载；未进行全模块性能基准 |

活跃需求引用见 `traceability.yaml`；验收未闭合项保留 `partial`，具体差距以 CR-025 为准。后文中旧的 done/partial/missing 仅描述其标注日期，不反推当前状态。

## 历史盘点

> [!info] 口径
>
> 审计时间 2026-08-17（M4 第一批后）。done 均有代码文件证据；partial 注明差距；missing 为未实现。
> 本次（2026-09-02）只补 20–22 三段，其余模块口径仍是 2026-08-17。三段按 [CR-010](../07.需求变更/10.CR-010-贾维斯可打断语音与常驻开关.md) 的 M0–M7 实际落点盘点；真机语音、审批、签名安装与延迟指标已在 CR-010 §6.8 回填。

## 02.书库与内容导入

完成 7 · 部分 6 · 缺失 9 · 新增 3

### ✅ 已完成
- FR-01(epub 部分)：epub 上传→arq worker 异步解析→状态可见（pending/parsing 徽标 3s 轮询）— app/routers/books.py、worker/tasks.py parse_book、web/features/shelf/ShelfPage.tsx
- FR-02(粘贴/URL 部分)：粘贴文本同步入库即读；URL 服务端 httpx 抓取 + trafilatura 正文提取（含老式 <br> 分段页修复与表格布局剥竖线）— app/routers/articles.py、domain/articles.py
- 解析管线服务端一次落库：ebooklib+lxml 分章分段（保留 heading/quote/code kind、spine 多章按 h1-h3 拆分）→ pysbd 分句 → UTF-16 词元标注 → 落库，书籍章节与独立文章共用同一 persist_paragraphs；代码块不参与词元与分句（FR-07 前半）— domain/epub.py、domain/segmentation.py、domain/articles.py
- FR-06：客户端不自行分句，阅读器直接消费服务端句子区间；偏移为 UTF-16 码元，含增补平面映射且有单测 — domain/segmentation.py、tests/test_segmentation.py、web/features/reader/ProseView.tsx
- FR-09(除进度条)：epub 内嵌封面抽取自托管（covers/{id}.img + GET /books/{id}/cover）；无封面时程序化封面按 slug 确定性哈希取色 — worker/tasks.py、app/routers/books.py、web/features/shelf/BookCard.tsx
- AC-02：前端自绘渲染直接消费服务端段落/句子数组，点击命中正确 sentence 记录（以架构简化替代方案中的 foliate-js+CFI 双轨映射，验收目标达成）— web/features/reader/ProseView.tsx、app/routers/books.py article_payload
- AC-04：URL 文章记录 source_url 与 fetched_at，同 URL 已落库直接复用不重新抓取 — domain/models.py、worker/tasks.py ingest_article、app/routers/articles.py

### 🔶 部分完成
- FR-05：失败保留原件/原始输入与错误原因（书 status=failed+error、前端点击可见），但书籍/文章无重试端点（仅视频有 /retry），失败只能重新导入 — worker/tasks.py、BookCard.tsx
- FR-07/08 容错半套：短段落保留有单测（空块才丢）；但无段落级失败标记（解析异常整本/整篇挂 failed），切章失败仅静默回退 "Chapter N" 标题无报告机制 — domain/epub.py、tests/test_segmentation.py
- FR-10 书籍详情目录：章节列表+当前章高亮+段数已有，缺字数/预计时长/已读态，分卷折叠未做（is_section 字段仅预留无读写）— web/features/reader/Toc.tsx、app/routers/books.py
- FR-13 删除文章：window.confirm 二次确认并说明生词保留，但未说明批注/进度连带（相关模块未建），原生弹窗在治理文档自列为遗留 — web/features/shelf/ArticleSection.tsx
- AC-01：同文件两次导入切分一致由确定性管线+重解析幂等（清旧重建）保证；"300页<60s" 仅治理文档自报（傲慢与偏见 6s/78章）未独立复测 — worker/tasks.py parse_book
- 数据要点偏差：article 表兼任 chapter、词元以 JSONB 内嵌 paragraph 不建 token 独立表（有意简化，UTF-16 锚点口径未受影响），chapter/token/shelf_item/reading_progress 四表未建 — domain/models.py、migrations/versions/

### ❌ 未实现
- FR-01 后半：pdf（文本层）/txt/md 导入未做，上传端点显式 400 "仅支持 epub"（books.py:25）
- FR-02 后半：文件导入文章、Markdown 专门解析、抓取失败回退查看原始 HTML 快照均未做（无 raw_key 字段与快照落盘）
- FR-03：内置公版书目（Gutenberg 3-5 本含封面/目录/字数/时长）完全未内置，book.source=builtin 仅预留默认值
- FR-04：超长内容字数提示与按章拆分/截断显式选项、扫描版 PDF 提示均未做
- FR-11 + FR-09 进度条：书级进度聚合完全缺失（无 reading_progress 表、无进度接口、封面无进度条）
- FR-12：文章库筛选/排序/标签/PostgreSQL FTS 全文检索/分页全部未做，list_articles 仅全量时间倒序（服务端无任何 FTS/tsvector 代码）
- 归属模型与 BR-02：书库/书架分离、shelf_item 引用表、显式加入书架、在读/未开始/已读完三段分组、移出书架保留进度——整套未实现，现"书架"页=全部导入书平铺列表（ShelfPage.tsx）
- BR-04 阅读来路枚举（书库/书架/文章决定返回文案落点）与 BR-05 阅读计时（前台交互时长增量上报）均未做，前端无任何计时/来路代码
- AC-03：删除书籍端点不存在（仅文章可删，books.py 无 DELETE），批注/进度存在时拒绝并报数的保护逻辑无从谈起

### ➕ 超出需求新增
- 整章逐句预翻译异步任务（缓存优先、断点续传）+ 句级译文批量查询接口 — app/routers/books.py /translate、/translations，worker/tasks.py translate_article
- 同 URL 重复导入幂等复用（直接返回已有文章 existed:true）+ 文章词数聚合统计返回 — app/routers/articles.py
- 书内记住上次阅读章节自动续读（localStorage ln-last-article-{bookId}）— ShelfPage.tsx、ReaderPage.tsx

## 03 精读阅读器与点词学习卡

完成 9 · 部分 9 · 缺失 7 · 新增 5

### ✅ 已完成
- FR-04 点词链路：词元 span 点击→高亮+朗读+学习卡一体触发（ProseView.tsx handleWordClick、WordCard.tsx、lib/audio.ts playTts）
- FR-04 句子面板内容：译文（bing/google/llm/auto 降级链）+ LLM 语法成分高亮与图例（SentencePanel.tsx、app/routers/analyze.py、domain/translate.py）
- FR-07 重复点击同词/句不重建面板、换句重建丢弃旧语法分析（WordCard.tsx key=段落+起点与会话缓存、SentencePanel.tsx key=sentence.hash）
- FR-11 三种正文模式（原文/双语/译文）切换即时生效并按视口首段锚点保持滚动位置（readerStore.ts viewMode、ReaderPage.tsx changeViewMode、ProseView.tsx 三态渲染）
- BR-02/BR-03 查词链路：本地 ECDICT 即查即显、AI 字段显式触发按 ADR-006 内容指纹落库、cached 命中直出且 AI 区与词典区分离不互洗（app/routers/dict.py、analyze.py、domain/analysis.py content_key/save_result、WordCard.tsx 分区渲染）
- BR-06 朗读队列单例：全局单 Audio 新播停旧，点词/点句朗读打断整章连读（lib/audio.ts、playerStore.ts chapterInterrupt+generation 代际计数）
- BR-07/BR-08 已实现层：朗读句底色、选中词底色加粗、生词单一虚下划线三层互不冲突，已收藏词批量查询后正文轻标记（styles/tokens.css .s-reading/.w-selected/.vocab、ReaderPage.tsx vocabStatus、app/routers/vocab.py /status）
- FR-13 核心+AC-03：整篇逐句异步翻译按 content_hash 缓存命中跳过、断点续传不重复计费，前端轮询流式回填含骨架占位（worker/tasks.py translate_article、app/routers/books.py translations、ReaderPage.tsx refetchInterval）
- FR-14 核心+AC-04：整章连读控制条含上/下句/倍速/音色，当前句高亮自动滚动跟随，手动滚动暂停跟随并有回到朗读位置，点词打断旧队列（PlayerBar.tsx、playerStore.ts、ProseView.tsx readingSentenceId、app/routers/tts.py 落盘缓存）

### 🔶 部分完成
- FR-01 三栏骨架（目录/正文/右面板）在，但无栏宽拖拽与记忆、无分隔条键盘调节、无面板折叠转浮动卡、无字号行距行宽调节（ReaderPage.tsx 固定布局）
- FR-02 仅右侧常驻面板单形态，无锚定弹卡形态与用户偏好切换，未引入 Floating UI（web/package.json 无 floating-ui 依赖）
- FR-04 点句半条：点整句出面板+高亮但不自动朗读、无行号区/句边距专用触发区，且译文与语法均需手动点按加载（ProseView.tsx selectSentence、SentencePanel.tsx）
- FR-06 学习卡首屏（词形/音标+播放/词典释义/词性/原形/考试标签）在，展开区其他义项、词形变化、搭配、成对例句、语法作用、标熟全部缺失（WordCard.tsx）
- FR-13 触发方式不符：进双语/译文模式隐式自动入队而非显式触发+消耗预估，翻译从章首顺序执行而非视口段起步，失败句无单独标记重试（ReaderPage.tsx enqueueTranslate、worker/tasks.py）
- FR-14 小缺口：连读起点固定章首（index 0），未实现从当前选中句起播（playerStore.ts setChapter）
- BR-01/BR-02 半条：词典 ECDICT 与 AI provider/model 署名齐全、查不到明示未收录，但无候选词形建议（仅去 s 后缀回退），且卡片打开不并发取回服务端已存 AI 结果、需手动点击才命中缓存（app/routers/dict.py、WordCard.tsx analyze 为手动 mutation）
- AC-01/AC-02 性能项：本地词典+分析缓存架构支撑但无 P95 度量与 AI 流式后补，无虚拟化下万词文章流畅仅治理基线自报实测（无 web 端性能测试）
- 旧版五类静默错误回归测试仅 1/5：哈希确定性有测（tests/test_analysis_key.py），例句错位/双重 percent-encode/空串 URL/词组点击区四类无对应测试

### ❌ 未实现
- FR-03 正文虚拟化与段落复合 key 记忆化：未引入 TanStack Virtual，ParagraphView 未 memo、无指纹 key（全量渲染）
- FR-05 学习卡两级模型：无替换式速查小卡、无卡内点词替换、前端无任何 Escape 分级关闭处理
- FR-08+BR-05 词组整链路：无拖选词组、无 ECDICT 短语离线拆解与标记开关、服务端无 phrase 相关代码/表，且客户端对路径参数自行 encodeURIComponent 与 BR-05 约定不符（lib/api.ts dict）
- FR-09/FR-10 自建右键菜单与选中即译浮层均未实现（阅读器无 contextmenu 与 selection/mouseup 处理）
- FR-12 译文插入样式配置与实时预览：.trans 样式硬编码，无按句/按段、下划线/背景/模糊等任何可配项（styles/tokens.css、app.css）
- FR-15+AC-05 批注系统整体缺失：多色高亮、文字批注、筛选、Markdown 导出、段落ID+字符区间锚定与防漂移，前后端及 migrations 均无 annotation 痕迹
- BR-04 例句与译文成对存取无从谈起（学习卡无例句功能），BR-08 后半标熟状态缺失（vocab 表无 mastered 字段与接口）

### ➕ 超出需求新增
- 按文章记忆并恢复滚动进度（ReaderPage.tsx localStorage ln-scroll-*，部分覆盖模块目标的进度自动保存）
- 词库出处跳转 ?sid= 定位目标句并 3 秒高亮（ReaderPage.tsx jumpSid）
- 空格/左右方向键控制整章连读播放（ReaderPage.tsx 全局 keydown）
- 连读时预取下一句 TTS 音频降低句间延迟（playerStore.ts fetch 预取 + tts.py 落盘缓存）
- 书内最近打开章节记忆（ReaderPage.tsx ln-last-article-*）

## 04.翻译编排与供应商

完成 9 · 部分 8 · 缺失 8 · 新增 3

### ✅ 已完成
- FR-02 单词/短语查询走本地 ECDICT 词典、不经翻译供应商（server/app/routers/dict.py、web/src/features/reader/WordCard.tsx）
- FR-02 句子点译按用户当前选定引擎调用、默认 auto 免费档优先（server/app/routers/analyze.py、web/src/features/reader/SentencePanel.tsx）
- FR-02 整篇翻译异步批量执行、逐句缓存跳过、断点续传（server/worker/tasks.py translate_article、server/app/routers/books.py）
- FR-02 AI 释义/语法经 LiteLLM 别名 translate-fast/explain-standard/grammar-deep 路由（server/domain/llm.py、server/domain/translate.py）
- AC-01 免费档全失效时 auto 链降级到 LLM 仍可用且标注实际引擎，测试覆盖（server/domain/translate.py FALLBACK_CHAIN、server/tests/test_translate_free.py）
- 实现方案·引擎抽象：统一契约 translate(text, engine, lang_pair)->{text, engine}（server/domain/translate.py）
- BR-01 前半：翻译结果按内容指纹落库，同句同引擎命中缓存不重复调用（server/domain/analysis.py、migrations/versions/fbd1c3df1fb2_initial_core_tables.py）
- BR-03 密钥只存服务端（LiteLLM 网关配置在 server/app/config.py 与 deploy/docker-compose.yml，前端无任何密钥）
- BR-04 整篇/整轨翻译用 arq _job_id 幂等防重复入队，重跑靠句级缓存跳过避免重复计费，LLM 客户端 max_retries=0（server/app/routers/books.py、server/worker/tasks.py）

### 🔶 部分完成
- FR-03 引擎切换：可切引擎重译并标注引擎/缓存态，但无多引擎并列对比、无生成时间标注、场景不记住上次选择（SentencePanel.tsx 每次挂载重置为 auto）
- BR-01 后半/AC-02：所有机器翻译共用 provider="mt" 单一缓存桶，换引擎重译使旧版本 is_active=False 被覆盖，换引擎不产生并存缓存行、无法三引擎对比（analyze.py MT_PROVIDER、analysis.py save_result）
- FR-04 降级链：仅 engine=auto 时降级；用户显式选定引擎失败即报错，无同档→下档自动回退，也无按引擎"禁止自动回退"开关（translate.py、test_single_engine_no_fallback）
- FR-06 调用记录：仅落 latency_ms 与 model（实际引擎），cost_micros 字段建了但从未写入，无错误分型/缓存命中/按次调用日志（domain/models.py AnalysisResult、analyze.py）
- 免费档覆盖：只接了 bing/google 两个免费引擎，需求列的有道/百度未接入，且治理基线自报 bing 端点已失效（translate.py FREE_ENGINES）
- BR-02：失效自动降级与错误提示已有，但 UI 无"免费通道，稳定性不保证"标注（SentencePanel.tsx）
- BR-05：缓存表有 lang_pair 维度（默认 en->zh），但没有按英中/中英方向的质量与可用性记录
- FR-02 整篇翻译 AI 档预估消耗：前端触发固定 engine=auto，无引擎选择入口也无消耗预估（web/src/lib/api.ts enqueueTranslate、ReaderPage.tsx）

### ❌ 未实现
- FR-01 能力注册表：无供应商能力声明、无连通性探针接口、无设置页展示探针结果
- 官方档整档缺失：DeepL/微软 Translator/有道智云均未接入（server 依赖与代码中无任何官方 SDK 痕迹）
- 自托管档 DLX：deploy/docker-compose.yml 无该服务，连注释的可选 profile 都没有
- FR-05 译文语气全局偏好：前后端代码无 tone/语气任何实现
- 实现方案·熔断：无连续失败挂起机制，Redis 中无熔断状态
- BR-06 适配完成/探针通过/启用三个独立状态及不可用原因展示：未实现
- AC-03 探针页区分网络不通/密钥无效/配额耗尽三类失败：无探针即无从谈起
- AC-04 用量页按供应商×能力聚合次数/延迟分位/费用：无任何用量接口或前端页面

### ➕ 超出需求新增
- 双语/译文阅读模式：整篇译文 3s 轮询回填 + "已翻译 n/m 句"进度提示（ReaderPage.tsx、ProseView.tsx）
- 视频字幕轨整轨翻译复用同一降级链与句级缓存，产出独立中文翻译轨（worker/tasks.py translate_track、app/routers/videos.py）
- 语法分析结果附带整句中文翻译，句面板双通道展示（domain/llm.py grammar_prompt、SentencePanel.tsx）

## 05.AI解释与语法分析

完成 10 · 部分 7 · 缺失 8 · 新增 3

### ✅ 已完成
- BR-01前半/AC-01路径：analysis_result 表按 ADR-006 六元组 (scope,content_hash,context_hash,kind,provider,lang_pair) 寻址，含 version/is_active 与部分索引 ix_analysis_lookup，命中走索引直读（server/domain/models.py、migrations/versions/fbd1c3df1fb2_initial_core_tables.py、server/domain/analysis.py）
- BR-05：content_key 做 NFC+空白折叠规范化 sha256 指纹，原文重导入后旧分析 hash 自然失配失效，无需清理（server/domain/analysis.py content_key）
- word_explain kind：POST /analyze/word 按 (word, context) 双 hash 寻址，命中返 cached=true，未命中走 explain-standard 别名生成落库（server/app/routers/analyze.py、server/domain/llm.py word_explain_prompt）
- grammar kind：POST /analyze/grammar 输出整句翻译/主干/成分数组/时态语态/难点字段（server/app/routers/analyze.py、server/domain/llm.py grammar_prompt）
- FR-01前半：AI 解释与语法分析均需点按钮显式触发，不随点词自动调用（web/src/features/reader/WordCard.tsx 'AI 解释'按钮、SentencePanel.tsx '语法分析'按钮）
- FR-03前半：'重新分析'常驻弱化按钮以 refresh=true 强制重析，save_result 旧版本置 is_active=false、version 递增置为当前（WordCard.tsx/SentencePanel.tsx、server/domain/analysis.py save_result）
- 实现方案-路由：LLM 调用经 LiteLLM 网关语义别名 + JSON mode（ADR-004），治理基线自报 explain-standard/grammar-deep 已绑真实模型且实测 cached:true，与代码一致（server/domain/llm.py complete_json）
- 语法结构展开层展示：成分角色着色高亮 + 图例（主谓宾状色块映射）（web/src/features/reader/SentencePanel.tsx ROLE_CLASS/roleLegend）
- AC-04词级：视频学习页复用同一 WordCard，同 (word,context) 跨书库/视频命中同一条服务端缓存（web/src/features/video/VideoLearnPage.tsx import WordCard）
- BR-04：AI 结果只存 analysis_result，词典释义与 AI 语境释义分区并列展示，不写回词典事实层（web/src/features/reader/WordCard.tsx 两个 wc-sec 分节、server/app/routers/dict.py 独立只读）

### 🔶 部分完成
- FR-01后半：'已有缓存打开时直接展示'仅靠前端会话级 Map（transCache/gramCache），刷新页面后仍需再点一次按钮才命中服务端缓存（SentencePanel.tsx、WordCard.tsx）
- grammar 三级结构：只有单级扁平字段（快速理解+成分+时态），深度解释层（歧义候选/相似句型/可替换表达/常见错误）prompt 与展示均未覆盖（server/domain/llm.py grammar_prompt）
- BR-01后半：cached=true 已返回，但费用记账未做——cost_micros 列建了但全库无任何写入，无用量流水（server/domain/models.py:136）
- BR-06：kind→别名绑定为代码硬编码（explain-standard/grammar-deep），无按 kind 可配置绑定、无失效回退默认别名逻辑（server/app/routers/analyze.py）
- AC-04句级：后端寻址支持同句跨模块命中，但字幕/视频侧没有句子语法分析入口（video 目录无 SentencePanel/api.grammar 调用），链路走不通
- FR-06：陪读页仅逐句翻译走 /analyze/translate 共享缓存，快捷提问产生的解释未落 analysis_result（web/src/features/talk/TalkSessionPage.tsx 只调 api.translate）
- word_explain 输出字段：语气色彩、近义辨析未作为独立字段，仅在 explanation 里笼统带过（server/domain/llm.py word_explain_prompt）

### ❌ 未实现
- FR-02：SSE 流式生成与中途取消完全未做，三个端点均为一次性 JSON POST，无 text/event-stream（server/app/routers/analyze.py 全文无流式）
- FR-03后半/AC-02：切换模型重析与多版本并存对比未做——版本在库里共存但无版本列表/切换 API，前端无版本切换与当前版本标识 UI
- FR-04：成分标注点击定位回原文词元未做，成分仅在面板内高亮，与原文 token 无关联交互
- FR-05：歧义句候选解释未做，grammar prompt/结果无 candidates 类字段，输出为唯一答案
- BR-02：GET/POST 读写分离未做，analyze 路由无只读 GET 端点，'看看分析过没有'无法不触发生成地探测
- BR-03/AC-03：LLM 输出无 JSON Schema/Pydantic 结构校验、无失败自动重试、无'部分成功'状态；json.loads 失败直接抛错，用户看到失败提示而非原始文本+结构化失败标识（server/domain/llm.py complete_json）
- BR-07：prompt 模板版本化入库未做，prompt 硬编码在 llm.py，结果只记 model 不记 prompt 版本
- kind 覆盖缺口：sentence_deep（句子精讲）、phrase_explain（词组解释）、summary（整篇摘要）三种 kind 及 summary-long 别名均无端点无 prompt；spaCy 词性/依存辅助交叉校验（可选项）也未做

### ➕ 超出需求新增
- translate kind（需求表未列）：句子机器翻译也统一走 analysis_result 缓存，MT_PROVIDER 桶内多引擎降级链命中互通，且支持 bing/google/llm 引擎切换（server/app/routers/analyze.py analyze_translate、SentencePanel.tsx 引擎下拉）
- AI 网关未配置时的 503 优雅降级提示（'AI 网关未配置，暂不可用'），前端区分网关缺失与普通错误（WordCard.tsx/SentencePanel.tsx errorHint）
- 分析结果记录 latency_ms 延迟指标（server/domain/analysis.py save_result）

## 06.朗读与语音中心

完成 8 · 部分 7 · 缺失 6 · 新增 4

### ✅ 已完成
- FR-06 朗读队列全局单例：模块级单例 current 音频 + playTts/playUrl 先 stopTts，点词/点句/整篇/背单词共用不重叠（web/src/lib/audio.ts）
- FR-07 整篇朗读主体：句级顺序合成播放、上一句/下一句/暂停/继续/倍速齐全（web/src/features/reader/playerStore.ts、PlayerBar.tsx）
- FR-07 高亮跟随：当前句 .s-reading 高亮 + scrollIntoView 自动滚动，wheel/touchmove 手动滚动后 setFollow(false) 暂停跟随（ProseView.tsx:52-182、PlayerBar.tsx:60-63、ReaderPage.tsx:305-311）
- FR-10 音频落盘缓存：按 sha256(text|voice|rate) 存 media_root/tts/*.mp3，命中直接 FileResponse 不再合成，即 AC-04 成立（server/app/routers/tts.py:_cache_path/synthesize）
- FR-10 整篇预取：播放当前句时 fetch 下一句触发服务端合成落缓存（playerStore.ts:69-71）
- AC-02 打断互斥：点词/点句触发 chapterInterrupt 暂停整章队列，词读完不自动恢复，需用户显式继续（audio.ts:29-32、playerStore.ts:170-178）
- BR-04 音素级朗读不承诺：全站无 IPA 朗读入口，发音按钮只读单词/句子原文，未把音标交给 TTS（WordCard.tsx:127、SentencePanel.tsx:105）
- 背单词发音链路：学习/复习卡自动+手动发音，autoplay 开关持久化（vocab/LearnPane.tsx:53、ReviewPane.tsx:72、vocab/shared.ts:68-73）

### 🔶 部分完成
- FR-01 供应商三档：仅免费档 edge-tts 一档落地（server/app/routers/tts.py），OpenAI/Azure/Kokoro/Web Speech 兜底全部缺失
- FR-02 音色目录：/tts/voices 实时拉 edge-tts 列表（名称/性别/locale）仅进程内缓存，无数据库化、无探针刷新、前端无试听按钮，BR-01 的可用性探针同缺（tts.py:47-60、PlayerBar.tsx:48-57）
- FR-04 语速：仅整篇朗读有前端 playbackRate 倍速且不持久化，单词/句子无语速、无音量调节、无实时试听、无分场景记忆（playerStore.ts:cycleRate、audio.ts）
- FR-07 句间停顿：写死 GAP_MS=300 常量，不可配置（playerStore.ts:21）
- FR-09 自动朗读：仅背单词场景有 autoplay 开关；翻译/查词完成后自动朗读及 30 字符阈值未实现，查词发音全靠手动点击（WordCard.tsx、SentencePanel.tsx）
- BR-03 控件状态：有 playing/paused/idle 三态文案，但无加载态，播放失败静默（playTts catch 空、error 事件直接跳下一句），无具体原因与重试（PlayerBar.tsx:75-82、audio.ts:40-42、playerStore.ts:85）
- AC-01 起播延迟：缓存命中链路存在但无任何性能度量/压测证据，P95 指标无法核实

### ❌ 未实现
- FR-03 场景音色绑定：查词/句子/整篇/背单词共用一个 localStorage ln-voice 全局音色，无按场景绑定（audio.ts:5-17）
- FR-05 来源策略：无供应商优先级/仅本地/仅指定策略配置，也无实际音源标注，后端单供应商失败即 502
- FR-08 页面不可见处理：全代码库无 visibilitychange 监听，切后台继续/暂停不受控
- FR-11 缓存治理：无 LRU 上限（2GB）、无清理任务（worker/tasks.py 无 TTS 相关任务）、无生词/收藏句优先保留，缓存只增不减
- BR-02/AC-03 降级链：edge-tts 失效无自动降级下一档、无用户提示，前端 audio error 被当作正常句尾静默跳过，属伪静默失败
- 音色目录数据库化：migrations/ 无任何 voice/tts 表，音色与场景绑定均未入库

### ➕ 超出需求新增
- 文本长度上限校验：超 2000 字符返回 400，防滥用（tts.py:14,30-31）
- 口语陪练模块复用 TTS：talk 回合返回 tts_url 走同一 /tts 缓存链路（web/src/lib/api.ts:374-376）
- 朗读控制条整体显隐开关（顶栏喇叭）与关闭即停（playerStore.ts:toggleVisible）
- 阅读位置记忆与回到朗读位置按钮（ReaderPage.tsx:268、PlayerBar.tsx:locate）

## 07 AI实时语音对话与陪读

完成 7 · 部分 11 · 缺失 5 · 新增 2

### ✅ 已完成
- FR-02/BR-01 打断核心链路：火山端 VAD 自动回合 + user_start 信号即时 flush 本地播放队列停声（server/app/routers/realtime.py、web/src/features/talk/RealtimeSessionPage.tsx、realtimeAudio.ts）
- FR-03 双方语音增量实时字幕：ASR interim 覆盖式更新 + AI 回复 delta 追加（server/app/routers/realtime.py、web/src/features/talk/RealtimeSessionPage.tsx）
- FR-04 服务端强制会话上限：30 分钟总时长 / 3 分钟空闲 watchdog，到点提示并安全结束（server/app/routers/realtime.py MAX_SESSION_SECONDS/IDLE_SECONDS）
- BR-02 凭证安全：火山 app_id/access_key 仅存服务端，浏览器经 WS 中继零密钥（server/domain/volc_realtime.py、server/app/routers/realtime.py）
- 场景库 10 个 YAML 场景（问候/点餐/问路/酒店/面试/会议/机场/购物/就医/电话），含角色设定与目标，测试覆盖加载完整性（data/scenarios/*.yaml、server/tests/test_talk.py）
- FR-09 核心与 FR-10 前半：双方分轨转写按回合落库、会后总结（做得好/建议/重点词组）、词组一键收藏入词库、历史会话列表与回放浏览（server/app/routers/realtime.py _persist_turns、server/domain/talk.py summarize_session、web/src/features/talk/TalkSessionPage.tsx SummaryCard/ReviewSession、TalkScenariosPage.tsx）
- 前端连接状态机（connecting/listening/speaking/ended/error）与说话状态波形可视化、麦克风/自动播放受阻的错误提示（web/src/features/talk/RealtimeSessionPage.tsx、realtimeAudio.ts）

### 🔶 部分完成
- FR-01 会话准备只有场景卡片/难度/实时-文字入口，缺双语辅助强度选项与预计供应商、预估费用级别展示（TalkScenariosPage.tsx）
- FR-02 realtime 模式无可选按键说话，按住录音只存在于回合制页（TalkSessionPage.tsx）
- FR-03 缺逐句中文提示与关键词标注；口语纠错仅回合制逐回合 feedback 有，realtime 回合完全无纠错（domain/talk.py、realtime.py）
- FR-04 时长/空闲阈值硬编码不可配，且无网络抖动重连状态展示（realtime.py、RealtimeSessionPage.tsx）
- FR-05 关麦/停音频/关远端会话（FinishSession）与回合落库齐全，但会后总结需用户手动点『生成总结』而非自动生成（RealtimeSessionPage.tsx teardownAudio、realtime.py finally、TalkSessionPage.tsx ReviewSession）
- FR-09/AC-04 纠错汇总未按语法/用词/表达三分类，无可复听关键片段——realtime 链路不保存任何音频（models.py audio_key 仅回合制用）
- FR-10 后半缺失：无删除整次会话与仅删原始音频的接口，音频/转写生命周期解耦无从实现（talk.py 仅有场景删除）
- BR-03/AC-02 _active_ws 互斥防重复连接、会话 ID 稳定，但断线重连（10s 内恢复）完全未实现，断即终结（realtime.py、RealtimeSessionPage.tsx 无重连逻辑）
- BR-04 有服务端时长上限但无费用上限与用量统计，治理基线自认『用量事件未接预算统计』（realtime.py，volc EVENT_USAGE_RESPONSE 定义了但未消费）
- BR-05/BR-06 未做音素发音评分符合边界、不可用时 503 明说原因，但纠错反馈未标注『AI 生成』、失败页只有返回按钮未引导文字陪读等替代路径（realtime.py、RealtimeSessionPage.tsx）
- AC-01 治理基线自报首响 710ms 达标（P50<1s），但打断 <200ms 与连续 10 轮稳定性均标注待真机人工验收，仓库内无延迟实测脚本佐证

### ❌ 未实现
- FR-06 阅读器内唤起陪读完全未实现：无文章全文/章节上下文注入会话，无阅读位置增量同步，reader 模块零 talk 集成（web/src/features/reader/ 无任何引用）
- FR-07 陪读提问模式未实现：无基于全文上下文的语法/词义问答，产生的解释按 ADR-006 落库并与点按分析共享缓存亦无
- FR-08 陪读纯文字模式未实现（现有文字对话是场景陪练，无文章上下文）；AC-03 陪读上下文验收随之无法进行
- 实现方案主路径 OpenAI Realtime WebRTC 直连 + ephemeral token 签发未实现（talk.py /talk/realtime/session 返回 410 废弃桩），实际以火山豆包服务端 WS 中继替代（架构偏离需求文档，应回写文档或 ADR）
- 备选管线 pipecat（Silero VAD + faster-whisper 流式 + LiteLLM + TTS）未实现，仅有回合制整段转写这一非流式变体

### ➕ 超出需求新增
- 用户自建场景完整 CRUD + AI 场景草稿生成（POST /talk/scenarios、/talk/scenarios/draft，server/app/routers/talk.py、domain/scenarios.py、web/src/features/talk/ScenarioEditor.tsx）
- 回合制语音/文字陪练完整链路：faster-whisper 录音转写、逐回合表达纠错反馈（level/note/better）、AI 回合 TTS 朗读、回合 ordinal 唯一约束防并发（talk.py、domain/talk.py、TalkSessionPage.tsx）

## 08 场景学习

完成 6 · 部分 7 · 缺失 4 · 新增 5

### ✅ 已完成
- FR-03 首批 10 个内置场景全部落地：data/scenarios/ 下 10 个 YAML（small_talk/restaurant_order/asking_directions/hotel_checkin/shopping_return/work_meeting/job_interview/airport_checkin/phone_call/doctor_visit），逐一对应需求清单十个主题（购物以退货场景实现）
- FR-04 场景编辑器全链路：新建/修改/删除 + AI 一句话生成草稿→表单确认→入库（server/app/routers/talk.py 的 /scenarios CRUD 与 /scenarios/draft；domain/scenarios.py generate_scenario_draft；web/src/features/talk/ScenarioEditor.tsx）
- FR-06 任一场景一键发起口语练习，场景目标/角色设定参数化注入会话 prompt，开场白作为回合 0：TalkScenariosPage.tsx start()、domain/talk.py build_system_prompt、routers/realtime.py build_realtime_system_role（回合制与实时链路均接入）
- BR-03 场景数据模型单一来源：回合制（talk.py）与实时（realtime.py）共用 domain/scenarios.py 的合并查询（内置 YAML + user_scenario 表），未建第二套场景数据
- AC-03 AI 草稿未确认不出现在正式列表：/scenarios/draft 只返回草稿不落库，用户确认后 POST /scenarios 才写入 user_scenario 表进入 list_scenarios_merged（talk.py、migrations/versions/d663477770bd）
- 场景 schema 强制校验：validate_scenario 校验必填/key 格式/CEFR 等级/关键句结构，草稿不合格抛 DraftInvalid，有单测覆盖（domain/scenarios.py、tests/test_scenario_validation.py）

### 🔶 部分完成
- FR-01 场景结构仅实现目标/角色/开场白/关键句/提示的精简 schema，缺对话轮次完整脚本（逐句译文/重点词组/发音）、可替换回答、文化礼貌提示、内容来源标记（domain/scenarios.py REQUIRED_FIELDS 仅 10 字段）
- FR-05 浏览学习只有会话页侧栏 ScenePanel.tsx：关键句可 TTS 朗读、提示可读，但无对话脚本阅读页、未复用点词/点句能力、无角色分轨音色（playTts 用全局单音色，tts.py 无角色-音色绑定，实现方案表中'角色朗读'项未做）
- FR-08 词组收藏：会话总结的重点词组可收藏进词库（TalkSessionPage.tsx collectVocab），但收藏对象是会话运行期产物而非场景词组内容，context_text 仅存英文原句、无场景语境标记；场景面板关键句无收藏入口
- AC-01 场景关键句可朗读但不可点词查询、不可收藏，与书库点读体验不一致（ScenePanel.tsx 仅朗读按钮）
- AC-02 前半达成（角色/目标注入 prompt，治理基线称实时链路实测通过），后半缺失：key_sentences 未注入会话 prompt，'刚看过的关键句被 AI 在练习中使用'无机制保障（domain/talk.py build_system_prompt 只用 title/role/goal）
- BR-01 仅有 is_builtin 区分内置/用户场景，AI 生成草稿确认入库后与人工手写场景无来源标识区分（sanitize_scenario 剥离所有非 schema 字段，无 source 字段）
- 实现方案'YAML 启动时同步入库'做成了启动直读内存（scenarios.py _load_all lru_cache）+ 查询时合并，功能等价单源但与文档描述有偏差

### ❌ 未实现
- FR-02 场景词组库完全未实现：无正式程度/语气/可替换表达/常见错误/例句字段，无频率+场景相关性排序（server 与 web 均无 phrase 内容实体）
- FR-07 '我读过的'素材联动栏未实现：无最近查询词句记录展示，也无喂给下一轮练习的通道（web/src 无相关组件，server 无 recent-lookup 接口）
- BR-02 医疗等敏感场景免责声明未实现：doctor_visit.yaml 无声明字段，前后端 grep 无 disclaimer/免责
- BR-04 可解释场景推荐未实现：无任何'学习内容→场景'推荐逻辑

### ➕ 超出需求新增
- 场景带 CEFR level 字段 + 三档表达难度（easy/medium/hard）注入 prompt，前端难度选择本地持久化
- 用户场景同 key 可覆盖内置场景（DB 优先合并），内置场景改/删返回 409 保护
- 会话结束自动生成总结（做得好/建议/重点词组），词组可朗读可收藏
- 自由话题（无场景）对话模式，含专属引导文案
- AI 草稿 key 撞内置自动加 _custom 后缀避免提交冲突（_normalize_draft）

## 09.视频与字幕学习

完成 13 · 部分 10 · 缺失 2 · 新增 4

### ✅ 已完成
- FR-01 YouTube 链路：URL 提交→yt-dlp 库直调下载（默认≤1080p）+ 标题/频道/时长/封面元数据 + 官方与自动字幕抓取（worker/tasks.py _yt_download、app/routers/videos.py）
- FR-02 网络配置：ytdlp_proxy 全局代理与浏览器 cookies 配置项、失败错误落库并有独立重试端点（app/config.py、worker/tasks.py、app/routers/videos.py retry_video）
- FR-03 本地上传：mp4/mkv/webm 上传入库并自动走转写管线（app/routers/videos.py upload_video、web/src/features/video/VideoLibraryPage.tsx）
- FR-04/AC-05 转写流水线：faster-whisper 句级时间戳+VAD、无官方英文字幕才转写、官方/自动/whisper 多轨并存可切换（worker/tasks.py _whisper_transcribe、ingest_video）
- FR-05/AC-04 整集机翻中文翻译轨：按句 content_hash 走统一分析缓存、断点续传、二次进入全缓存命中零新增调用（worker/tasks.py translate_track、domain/analysis.py）
- FR-06 主体：pending/downloading/transcribing/translating/ready/failed 状态机 + 0-100 进度上报前端轮询展示，转写失败保留已下载文件与错误原因可重试（worker/tasks.py、VideoCard.tsx、VideoLearnPage.tsx）
- FR-07 主体：<video> 原生播放 + starlette FileResponse 原生 HTTP Range 流式 + 倍速切换 + 上一句/下一句句级快退快进（app/routers/videos.py stream_video、VideoLearnPage.tsx）
- FR-08 部分：当前句高亮（二分定位+rAF/timeupdate 双通道同步）、字幕轨下拉切换、有中文轨时双语叠显（VideoLearnPage.tsx、videoUtils.ts findActiveCue）
- FR-09 字幕点读：侧栏完整字幕列表任意句点击跳转起播、当前句自动跟随滚动、手动滚动暂停跟随并可一键回位（VideoLearnPage.tsx SubRow/jumpToCue/follow）
- FR-10 字幕点词：cue 文本词元化成可点 span、点词出阅读器同款 WordCard 学习卡（复用 03 模块分析/收藏链路）、点词自动暂停可开关（CueWords.tsx、videoUtils.ts、VideoLearnPage.tsx handleWord）
- FR-11 部分：逐句暂停（句尾自动停、同句只停一次）与单句循环均已实现（VideoLearnPage.tsx syncPlayback/sentencePause/loopCue）
- FR-14 其他站点：仅校验 http(s) 前缀无站点白名单，任意 yt-dlp 支持站点同链路，失败原文报错（app/routers/videos.py create_video、worker/tasks.py）
- BR-02/BR-04/BR-05：有官方英文字幕不触发转写且自动字幕标注'自动'、时间戳毫秒存储、同 URL 幂等去重返回已存在提示（worker/tasks.py、domain/models.py SubtitleCue、app/routers/videos.py）

### 🔶 部分完成
- FR-01 分辨率不可选：写死 bv*[height<=1080] 格式串，无用户分辨率选项（worker/tasks.py）；且 FR-02 要求的失败原因分类（网络/地区/年龄限制/链接失效）未做，仅透传 yt-dlp 原始异常文本
- FR-04 词级时间戳未启用：transcribe 未传 word_timestamps=True，whisperX 强制对齐完全未接（worker/tasks.py _whisper_transcribe）
- FR-05 AI 逐句精译：后端 translate_track 的 engine 参数支持 llm，但前端只调默认 auto，无精译入口（app/routers/videos.py、VideoLearnPage.tsx translate）
- FR-06 各阶段独立可重试未达：retry 整条管线从头重跑（清全部字幕轨重新下载+转写），非分阶段重试（app/routers/videos.py retry_video、worker/tasks.py ingest_video）
- FR-07 倍速仅 0.75/1/1.25/1.5×，未覆盖需求 0.5-2x；无 5 秒快退快进（仅句级跳转）（VideoLearnPage.tsx RATES/nav）
- FR-08 无双语/仅英/仅中/隐藏四种显示模式切换：有中文轨即恒显双语，无模式开关（VideoLearnPage.tsx 全文无 displayMode）
- FR-11 无 AB 循环（全库 grep 无实现）
- FR-13 视频库：卡片有封面/标题/时长/处理状态，但无分页（list_videos 全量返回）、无学习进度/已观看区间展示（播放位置仅存 localStorage）、前端无删除入口（后端 DELETE 接口存在但一次性删除，无'视频文件与字幕/生词分别确认'）
- BR-01/BR-03：字幕句 content_hash 与 sentence 同口径且翻译/分析缓存跨模块命中，但 subtitle_cue 独立表未并入统一句子表族；转写轨仅标 'whisper' 未记模型名与版本；无字幕句就地编辑接口（编辑后缓存失效逻辑无从实现）
- AC-01/AC-02 性能指标（8 分钟入库、100ms 跳转/≤200ms 偏差）无测试佐证，静态审计无法核实；治理基线亦自报 YouTube 官方字幕入库待可用网络回归

### ❌ 未实现
- FR-03 后半：指定服务器路径导入未实现；外挂字幕文件（srt/vtt/ass）导入无任何端点，且字幕解析仅支持 vtt/srt，ass 格式不支持（domain/subtitles.py、app/routers/videos.py）
- FR-12/AC-03 生词视频语境：VocabOccurrence 仅 article_id/sentence_id/context_text，无 video_id/cue/时间戳字段；点词收藏只带 cue 文本，复习卡片无法跳回视频时间点（domain/models.py、app/routers/vocab.py、CueWords.tsx）

### ➕ 超出需求新增
- yt-dlp cookies-from-browser 配置过 YouTube bot 校验、播放列表链接自动取首条（worker/tasks.py）
- YouTube 滚动式自动字幕重复行合并去重（domain/subtitles.py _merge_rolling_duplicates）
- 播放位置 localStorage 记忆与恢复、空格/左右方向键快捷键、点词 TTS 发音且避免与视频声道叠音（VideoLearnPage.tsx）
- 翻译轨生成进度轮询与 cue_count 增量自动刷新、无音轨视频跳过转写防炸（VideoLearnPage.tsx、worker/tasks.py）

## 10.LLM动态配置与成本控制

完成 5 · 部分 6 · 缺失 9 · 新增 0

### ✅ 已完成
- 网关底座：LiteLLM Proxy 容器 + PostgreSQL 配置持久化（STORE_MODEL_IN_DB=True），供应商/模型/密钥存网关库，改配置无需重启应用（deploy/docker-compose.yml、deploy/initdb/01-litellm-db.sql）
- BR-02 业务代码只引用语义别名 translate-fast/explain-standard/grammar-deep，无硬编码供应商模型名（server/domain/llm.py、server/domain/translate.py、server/app/routers/analyze.py、server/domain/talk.py）
- BR-03 应用侧统一按 OpenAI 兼容协议经网关调用，无按供应商名硬编码分支（server/domain/llm.py）
- BR-01 密钥只存服务端：网关密钥经环境变量注入，仓库/前端/日志均无密钥（deploy/docker-compose.yml、server/app/config.py）
- 调用结果标注实际模型并落库（provider/model/latency_ms），前端展示 provider·model；缓存命中直读不触发 LLM 调用（server/domain/analysis.py、server/app/routers/analyze.py、web/src/features/reader/WordCard.tsx）

### 🔶 部分完成
- FR-01 供应商 CRUD：网关自带能力且治理基线自报已手工配好真实供应商，但需求要求的 api 封装管理接口 + 应用内增删供应商/模型自动拉取（/models）完全未做，app/routers/ 无任何 LLM 管理路由
- FR-06 两层限制：部署级上限仅剩 config.py:30 的 llm_hard_budget_monthly_usd 字段定义，全仓库无一处引用（零校验零执行）；月度/单供应商日额/实时语音日额三档运行时预算完全没有
- FR-10 调用记录：analysis_result 记 provider/model/latency/缓存命中，但无 token 数、估算费用、成败分型，且仅覆盖 analyze 类缓存调用，talk 等调用不入账（server/domain/analysis.py、server/domain/models.py）
- BR-04 降级安全性：网关不可达时 LLM 调用 503 拒绝、翻译降级免费引擎链，方向正确，但无最后已知配置版本/陈旧窗口机制（server/app/routers/analyze.py、server/domain/translate.py）
- AC-01 不重启生效：网关 DB 存储架构上支持热生效，但应用内新增供应商并改绑的操作路径不存在，验收步骤无法走通
- AC-02 fallback：结果已标注实际模型，但 fallback 链在代码与配置中均无体现，主故障自动切备未实现未验证

### ❌ 未实现
- FR-02 应用内模型设置页可视化改绑：web/src/features/ 仅 reader/shelf/talk/video/vocab 五模块，无 settings；lib/api.ts 无任何 LLM 管理接口
- FR-03 每别名可配 fallback 链与超时/重试参数：complete_json 硬编码 timeout=60、max_retries=0（server/domain/llm.py）
- FR-04/AC-04 能力测试：测一下按钮、真实调用+schema 校验、连通失败/超时/schema 不通过三类区分、结果入库，均无实现
- FR-05 Ollama 本地模型一等接入：全仓库（代码/compose/脚本）无任何 ollama 痕迹
- FR-07/AC-03 预算耗尽拒绝付费调用并提示替代路径：无预算机制，故无耗尽行为与替代引导
- FR-08 用量达 80% 阈值应用内预警
- FR-09 用量页额度视图/分析视图（供应商×能力×模型分组、P50/P95）：前后端均无，migrations 无用量表
- FR-11 配置变更审计（谁/何时/改了什么）与回滚：无审计表无接口
- BR-05 实时语音预算独立设限独立显示：volc_realtime.py:47 定义 EVENT_USAGE_RESPONSE=154 但从未处理，用量未记录（与治理基线自报遗留一致）

## 词库与背单词

完成 10 · 部分 7 · 缺失 1 · 新增 5

### ✅ 已完成
- FR-01 词典数据：ECDICT sqlite 全量导入 PostgreSQL，字段含音标/中英释义/词性/柯林斯/考纲标签/BNC+COCA 词频/词形变化，词频做五档文字化（server/scripts/import_ecdict.py、domain/models.py DictEntry、app/routers/dict.py freq_band）
- FR-06 背单词模式：看词识义（忘记了/模糊/认识三档+太简单）、拼写练习逐字母校验+错误抖动重来、发音自动播放可开关+手动按钮/P 键（web/src/features/vocab/ReviewPane.tsx、Flashcard.tsx）
- FR-07 学习统计：今日新学/复习量、待复习数、未来 7 天到期分布、词表 learned/total 进度，复习可随时退出/跳过（app/routers/review.py /review/stats、web/src/features/vocab/VocabPage.tsx）
- FR-08 FSRS 调度：py-fsrs Scheduler（FSRS-6，关 fuzzing）评分推进记忆状态、due_at 到期队列、每次评分落 review_log 全量保留 fsrs_log（domain/srs.py、app/routers/review.py、migrations/versions/0538bbc594b2_srs_columns_and_review_log.py、tests/test_srs.py）
- AC-02 收藏与学习闭环：收藏只写生词本和来源语境，不生成到期时间；完成学新词或显式加入计划时才幂等初始化 FSRS。复习卡继续展示最近语境与来源（app/routers/vocab.py、practice.py、review.py）
- AC-03 中断一致性：队列以服务端 due_at 为唯一事实，重进重取不重复；5 秒内重复评分去重（app/routers/review.py DEDUP_WINDOW_S）
- BR-01 查词/收藏/入队/掌握独立：查词纯只读，收藏写 `vocab_entry+occurrence`，入队才初始化 FSRS，掌握由正式评分间隔推进 status（app/routers/dict.py、vocab.py、practice.py、review.py）
- BR-03 语境拆表：`vocab_occurrence` 保存 `reader/video/talk/grammar/wordlist/practice/manual` 来源和稳定内部定位标识，同源幂等、跨源保留，复习和导出取最近语境（domain/models.py VocabOccurrence、app/routers/vocab.py、review.py）
- BR-05 词典与 AI 分栏：点词卡「词典释义(ECDICT)」与「AI 语境释义(provider·model·缓存标识)」分区展示，AI 仅手动触发从不自动调用（web/src/features/reader/WordCard.tsx）
- BR-02(主体) 生词本与收藏同一份数据：单一 vocab_entry 存储，前端「生词本」词表视图直接读同一列表（app/routers/vocab.py、web/src/features/vocab/VocabPage.tsx）
- FR-485~498 听读连播与每词换声音（2026-09-05）：底部停靠播放条、词 × N → 释义 → 例句、单词循环、网格联动、mediaSession；`word_voice` 表 + `ttsUrl` 显式 voice；新场景 `tts-meaning`（web/src/features/vocab/listenStore.ts、ListenBar.tsx、server/app/routers/tts.py）
- FR-499~506 本级维护与听读完善（2026-09-05）：按本清发音缓存（目录求交 + `tts.cache_epoch`）、听读算学习中（每词即时曝光，2026-09-05 第三轮由攒批改即时）、按本清进度（收藏保留）、AI 补全本内词条（分片续跑 / 取消 / 熔断）、键位表与帮助、释义念全部词性 / 回想停顿 / 念满就停、Topbar 深链返回 + 子面板回本、听读跨页常驻与断点（server/domain/deck_ai.py、tts_cache.py、study_reset.py、web/src/features/vocab/DeckMaintenance.tsx、ListenGlobal.tsx、listenResume.ts）
- FR-507~511 全局查词（2026-09-05）：⌘K 词典联想 + 词汇模块「查词」tab；三张离线侧表（联想 198 万 / 反查 92 万 / WordNet 同义关系 14 万）；英文精确 → 变形 → 前缀 → 词组 → 子串 → 同义反义派生 → 拼写建议，中文三档反查，通配；AI 辨析按指纹缓存（server/domain/dict_search.py、dict_search_build.py、dict_forms.py、dict_gloss.py、scripts/seed_dict_search.py、web/src/features/vocab/dict/、CommandPalette.tsx）

### 🔶 部分完成
- FR-02 点词三层降级：本地命中即显+AI 手动按钮已做；拼写纠错 / 词形候选现由查词链提供（`/dict/search` 的 suggestions 与 forms，FR-508），但词卡未命中时还没把候选列在卡上、也没有跳查词页的入口（WordCard.tsx dictMissing）
- FR-03 词形还原：`exchange` 七个码与后缀回退已在 domain/dict_forms.py 收齐并接进查词链，`GET /dict/{word}` 仍只做去 s 回退、未接 spaCy lemmatizer 与候选裁决（app/routers/dict.py、WordCard.tsx lemmaOf）
- FR-04 内置词表：8 个考纲词表（中高考/四六级/考研/托福/雅思/GRE）以 ECDICT tag 虚拟词表实现且带进度，但牛津 3000 未提供（oxford 字段已导入未用）（app/routers/wordlists.py WORDLISTS）
- FR-09 复习卡语境：来源数据已统一保存来源类型、名称、内部定位标识和幂等指纹；书籍跳回完整，视频、对话、语法、词书和练习的统一「回到出处」交互待补（domain/models.py、app/routers/vocab.py、review.py）
- FR-10 生词状态：仅 new/learning/known 三档，缺「熟悉」「暂停」；且已掌握词在阅读器仍高亮——/vocab/status 不按状态过滤（domain/models.py status 注释、app/routers/vocab.py vocab_status、ProseView.tsx）
- BR-04 算法可查可回滚：卡片/日志按 py-fsrs 标准字典存储、Anki 可导出，但调度参数写死默认值，无版本查询与回滚接口（domain/srs.py 单例 Scheduler）
- AC-01 万级列表性能：词表用 offset 分页「加载更多」，虚拟滚动未做（WordlistPane.tsx 注释自认未装 react-virtual）；本地查询走主键应达标但无压测记录

### ❌ 未实现
- FR-05 + AC-04 自定义词表导入：CSV/TSV/JSON 解析→预览（新增/重复/失败统计）→确认→失败明细下载 全链路无任何实现，wordlist/wordlist_item 表也未建（migrations 与 routers 均无）

### ➕ 超出需求新增
- 生词本导出：CSV（BOM）与 Anki .apkg（genanki，固定 model/deck id + 稳定 guid 支持增量导入）——把 BR-04 的「未来可导出」提前落地（app/routers/export.py）
- 学习报告页：365 天复习热力图、30 天新学/复习双趋势、状态与来源占比、生词最多 top5 文章（app/routers/review.py /review/report、ReportPane.tsx）
- 学新词流：按词频取未学词，每个词完成学习答题后才初始化 FSRS，领取或收藏本身不计为已学（app/routers/practice.py、domain/practice.py）
- 复习全键盘操作：空格翻面、1-4 评分、S 拼写、P 发音、Tab 切换（ReviewPane.tsx）
- 连续学习天数（streak）统计与展示——需求明确「不做连续打卡压力设计」，此项超出且与需求意图相悖，建议产品确认（app/routers/review.py streak_days）

## 13.音标与发音训练（2026-08-19 落地后复核）

### ✅ 已完成
- FR-390 `word_phoneme` 主档：ipa-dict en_US/en_UK + CMUdict 重音音节 + g2p_en OOV 兜底，147,624 条，`source` 三分可辨（scripts/seed_phonemes.py、domain/models.py WordPhoneme）
- FR-391 `phoneme_difficulty`：speechocean762 标注解析出 39 个音素、94,445 个标注点，**只读标注不跑音频**（scripts/seed_phoneme_difficulty.py）
- FR-392 音位卡片：44 条（20 元音 + 24 辅音），例词按词首/词中/词尾从 CMUdict + 词频现算而非手写；21 张 CC0 剖面图；元音四边形自绘；关键部位高亮（domain/phoneme_cards.py、web/src/features/phonetics/）
- FR-393 听辨训练：identification 二选一，13 个对比组共 2,143 组词对，每题从 26 个母语音色里抽、组内 ≥6，答错并排回放 + IPA 差异位高亮，FSRS 直接复用 `domain/srs.py`（app/routers/phonetics.py、ContrastDrill.tsx）
- FR-394 结业进度：400 分钟进度条常驻音标页顶部
- FR-395 音标认读：看音标选词 / 看词选音标，干扰项从音素数相同的词里挑（DecodeDrill.tsx）
- FR-396 跟读打分：复用模块 09 链路，未新建（app/routers/shadowing.py）
- FR-397 音素分解：阅读器点词面板内联小控件（PhonemeStrip.tsx）
- FR-398a~c 第 0 层评测：完整度（纯文本比对）、流利度与词级置信度（CTC 强制对齐）；FR-398j 第 3 层 LLM 只叙述不打分（domain/pronunciation.py）
- ~~FR-398d~g / FR-398i 第 1/2 层（音素识别 + GOP）~~ **2026-08-30 下线**，见下
- ~~FR-399a~c 校准~~ **2026-08-30 下线**（达成过 ρ=0.687，产物存档）
- BR-92 ~ BR-94 落地：score 按帧数归一化；来源字段可见；跟读不进 SRS。BR-90/BR-91 随音素层失效

### 🔶 与需求文档不符（已在需求 §14 记录）
- ð/z 对比组按常规词频门槛只有 14 组——那是语言事实。给该组单独放宽词频得 37 组，未降低 AC 标准
- ~~第 1 层实际用 fp32 ONNX 而非 int8~~（层已下线）
- FR-396 跟读打分现在只出第 0 层三个指标：完整度、流利度、发音准确度（词级 CTC 置信度均值，未校准）

- ~~FR-392h 示范音频音素级时间戳~~ **2026-08-30 随音素模型一起下线**：它靠第 2 层的强制对齐切音素区间。`/phonetics/word/{word}/timings` 路由删除（前端零调用点），44/44 音位示范音已落盘走缓存不受影响；8 个双元音的示范音自此**不可再生**（Commons 没有，唯一来源是 TTS + 该模型对齐切段），`server/data/media/phoneme_ipa/` 从缓存升格为不可再生资产，存档在 `归档/2026-08-30-发音评分模型下线/`

### ❌ 未实现
- 无

## 14.语法学习与写作纠错（2026-08-19 落地后复核）

### ✅ 已完成
- FR-400 CEFR-J 目录：501 条（官方 xlsx 实际条数）全部入库并译中文，24 个范畴分组、乱序可进（scripts/seed_grammar.py、app/routers/grammar.py）
- FR-401 构式识别：55 条 spaCy DependencyMatcher 规则，扫全语料得 387,490 处真实出现，点进去跳原文（domain/grammar_rules.py、scripts/scan_grammar.py）
- FR-402 误区模型：36 条可命名误区，与 ERRANT 55 类双向映射，命中计数驱动排序（domain/misconceptions.py）
- FR-403 两段式纠错：LLM 出修正句 → ERRANT 原子化 → 逐条讲解；Harper 浏览器端即时红线且懒加载（domain/writing.py、WritingPane.tsx）
- FR-404 练习形式：五类题型全部落地——指称型（带 TTS 听力版）501、情感型 501（无对错、不进 SRS）、句型转换 501、词块排序 208、语料定位（由 occurrence 现出，零 LLM 成本）。共 1,711 张卡，**501 个语法点全覆盖**
- FR-405 可视化：默认成分着色（纯 CSS + span）、二级依存图（已装的 @xyflow/react + dagre）、服务端只返 JSON、45 条标签中文映射自写；现有 AI 弹窗保持不动
- FR-406 SRS：一题一卡、只调度可判定题、`domain/srs.py` 零改动
- BR-95 ~ BR-99 全部落地

### 🔶 按需求刻意不做
- FR-400c English Grammar Profile 补充素材：需求写明「个人自用不再分发」，当前未引入任何 EGP 内容
- FR-403f 二级规则复核（LanguageTool）：需求写明「默认不部署」，三条触发条件均未满足

### ❌ 未实现
- 无

## 练习引擎（组件，2026-08-19）

### ✅ 已完成
- 三层拆分：题目 Schema 校验（`validate`）、判分纯函数（`score`）、Widget 注册表（`register`）
- 12 种题型注册，`schedulable` 声明决定是否进 FSRS
- 误区匹配三形式（equals / contains / regex），未命中才降级 LLM
- 渲染层与判分层分离：前端 `Widgets.tsx` 不含任何答案比较
- 28 个单测覆盖判分，含「同输入必同输出」的纯函数约束

### ❌ 未实现
- YAML → JSON 内容编译管线：需求列为「内容量起来之后再做，早期直接写 JSON 也行」，当前走 LLM 出题 + Schema 校验入库

## 20 语音助理（已按 CR-016 退场）

当前状态：不再属于 NEXUS 产品范围。工作台入口、常听与唤醒、主动播报、专属语音协议和桌面辅助窗口已删除；普通口语练习、ASR 与 TTS 保留。以下条目仅作为 CR-008 至 CR-015 的历史实施记录，不再表示当前功能。

### 已完成
- BR-20-01 能力名：技能层 `assistant` 未绑回落 `chat-general`、内核 `computer-use` 经 `_route` 取绑定，未绑直接报错不回落网关——server/domain/assistant.py build_model、server/domain/jarvis.py `_route`
- BR-20-06 / 07 短回复系统提示词与调用台账（`source=assistant`，未解析路由记 `plugin_id=unresolved`）——server/domain/assistant.py、tests/test_assistant.py
- BR-20-08 工具目录与实际注册对账；`computer_task` 只把目标交给全局会话排队——server/domain/assistant.py TOOL_CATALOG / computer_task、tests/test_jarvis.py::test_ensure_reuses_one_thread_and_queue_policy
- BR-20-09 + FR-20-05 / 06 / 09 早报四段独立、例程表滴答、「今天」右列早报卡——server/domain/briefing.py、server/domain/routines.py、web/src/features/home/TodayPage.tsx
- BR-20-11 识别在本机：openWakeWord + 独立 Silero scorer，只推 16k PCM 到本机 API；`LocalVadProvider` 连续模式不套用唤醒词冷却——server/domain/listening.py、server/domain/jarvis_voice.py、tests/test_listening.py
- BR-20-17 插话三分法的引擎侧（M1）：`submit(policy)`、停词短句 interrupt、其余 steer、steer 被拒退 queue 再退本地队列、超时不重播上一轮——server/domain/jarvis.py submit / `_end_turn`、server/domain/jarvis_intent.py classify_intent、tests/test_jarvis.py（busy_steer / stop_word_interrupts_then_restarts / unsteerable_falls_back_to_queue / timeout_does_not_replay_previous_speech）、tests/test_jarvis_intent.py（5 条）
- BR-20-20 语音只认拒绝的判据——server/domain/jarvis_intent.is_decision_phrase、tests/test_jarvis_intent.py::test_voice_only_expresses_rejection
- FR-20-02 技能层 12 个工具（11 个工作台工具 + `computer_task`）——server/domain/assistant.py TOOL_CATALOG
- FR-20-11 设置 · 模型服务的「语音助理」「助理朗读」「电脑操控」「贾维斯语音」能力；`volc_duplex` 的 `api_key` 走加密凭据库——server/app/routers/config.py、server/domain/credentials.py
- BR-20-01 / FR-20-12 全双工：`jarvis-voice` 可独立绑定 Seeduplex 3.0，`VolcDuplexProvider` 支持 function calling、`response.cancel`、replacement 播报；未绑定回落旧 `realtime-voice`——server/domain/volc_duplex.py、server/domain/volc_realtime.py、server/domain/jarvis_voice.py、tests/test_volc_duplex.py、tests/test_jarvis_voice.py
- BR-20-17 / 19 语音侧打断：`VoiceOrchestrator` 是唯一裁决者，旧协议 450 / 459 + 500 分句念稿、全双工 speech_started、假打断 `tts.resume`、执行中三分法与多 function call 结果归属均已覆盖——server/domain/jarvis_voice.py、tests/test_jarvis_voice.py
- 火山实时会话启动失败时切 `LocalVadProvider`，经 `assistant.asr` 整段转写和 `LocalTtsSink` 播报；TTL 预建成功后在静音窗口回切实时会话，半连接先清理——server/domain/jarvis_voice.py、tests/test_jarvis_voice.py
- FR-20-12 统一语音 WS 与客户端：`/jarvis/voice/ws/{sid}`、`voiceClient.ts`、`jarvisStore.ts`、统一 PCM 采集器（worklet 内降采样 + Int16 + 攒帧，`setMuted` 只停 post）——server/app/routers/jarvis_voice.py、web/src/features/jarvis、web/src/lib/pcmCapture.ts
- 打包态会话恢复：发指令与开麦前总是调用 `/jarvis/session/ensure`，API 重启后从 store 的旧会话切到新执行句柄；壳每 5 秒复查 API 并在外部 API 退出后补起——web/src/features/jarvis/jarvisStore.ts、web/src/features/jarvis/jarvisStore.test.ts、desktop/src/main.ts
- FR-20-13 数据源：引擎事件 hub（每会话 ring buffer + seq + 订阅）与 SSE `GET /jarvis/sessions/{id}/events?after=N` replay 后订阅——server/domain/jarvis_events.py、server/app/routers/jarvis.py、tests/test_jarvis.py（stream_deltas_publish_then_persist / events_sse_replays_after_cursor）
- BR-20-15 / 16 三档真源：`GET|PUT /jarvis/mode`、`jarvis_mode.py`、托盘 radio、IPC 广播、登录项回读与电源降级已接通——server/domain/jarvis_mode.py、server/app/routers/jarvis.py、desktop/src/{main,mode,api,power}.ts
- BR-20-18 常听可见性：ALWAYS-ON 橙色状态、锁屏 / 休眠 / 电池 / 热降频原因经壳 IPC 同步到刘海与 HUD——desktop/src/power.ts、web/src/features/jarvis、web/src/components/JarvisHud.tsx
- FR-20-13 四个前台与壳辅助窗：主 HUD、刘海、overlay、审批页共用 `jarvisStore`；样式按 `data-window` 隔离——web/src/features/jarvis、web/src/components/JarvisHud.tsx、desktop/src/windows
- FR-20-14 / 15 设置分区、壳模式 API 与 `GET|PUT /jarvis/voice/settings` 已落——web/src/features/settings、web/src/lib/shell.ts、server/app/routers/jarvis_voice.py
- 已废入口已删：`AssistantHud.tsx`、`AssistantPage.tsx`、`assistantStore.ts`、`listenClient.ts`、`realtimeClient.ts`、`features/computer/*`；`routers/realtime.py` 不再承载贾维斯。`listening.follow_up` 仅保留给旧 `/assistant/listen` 兼容入口，不再是贾维斯主链路。
- M0 测试替身与只读协议探针——server/tests/fake_app_server.py、server/scripts/jarvis_probe.py（data/logs/jarvis-probe-20260902-*.json 五份）
- 真机语音与回声验收：Float32 24k 播放校正、ready 握手、本地能量 VAD 即时降级并后台升级 Silero、上游自动恢复；最终代码重启后普通回合首音频 629.0ms，抢话停声 P50 123.95ms / P95 124.8ms，AEC 20 轮结论 `full_duplex_ok`——server/domain/jarvis_voice.py、server/domain/listening.py、server/domain/volc_duplex.py、web/src/features/jarvis/voiceClient.ts、data/logs/voice-latency-20260902-163711.json、data/logs/aec-probe-2026-09-02.json
- CR-018 统一视觉系统：Image2 生成应用标识、16 个年轻化二次元业务领域插画、36 个二次元操作图形和纸张纹理；`BrandIcon`、`NexusVisual` 与 `NexusIcon` 覆盖全局导航、公共操作和全部路由背景——web/public/brand/image2、garden-gpt-image-2/source/anime-*-atlas.png、web/src/components/{BrandIcon,NexusVisual,NexusIcon}.tsx、web/src/App.tsx

- BR-20-24 前台八个只读事实工具（`front_desk_tools()` = 事实 + `computer_task`，判据「会不会改变任何状态」由 AST 扫描守着）——server/domain/jarvis_facts.py、tests/test_jarvis_facts.py（5 条）（2026-09-03 M87）
- BR-20-25 两层记忆：`data/jarvis/profile.md` 人设档案 + `user_pref['jarvis.memory']` 每轮一条情节（滚动 60），开线程时拼进 `developerInstructions`——server/domain/jarvis_memory.py、tests/test_jarvis_memory.py（9 条）（2026-09-03 M87）
- BR-20-26 ALWAYS-ON 待唤醒档：`WakeGate` 本机唤醒词，没叫到名字麦克风不上行，空闲自动退回；`agent.state` 多一档 `armed`——server/domain/jarvis_voice.py、web/src/features/jarvis/voiceClient.ts、tests/test_jarvis_voice.py（4 条）（2026-09-03 M87）
- BR-20-27 主动播报：例程产出先交在线语音会话念，接不下经 SSE 0 号广播位转壳通知——server/domain/routines.py、server/app/routers/jarvis_voice.py、web/src/components/ShellBridge.tsx、tests/test_jarvis_announce.py（5 条）（2026-09-03 M87）
- CR-012 统一身份、conversation、turn、mission、command、event、memory 与 announcement 持久对象，文字、语音和任务共用 Kernel 投影——server/domain/jarvis_kernel.py、server/domain/jarvis_missions.py、server/domain/models.py
- CR-012 前端按全局 cursor 补齐持久事件；语音帧和播放回传携带 trace/conversation/turn/mission 及时序——web/src/features/jarvis/jarvisStore.ts、web/src/features/jarvis/voiceClient.ts、server/domain/jarvis_voice.py
- CR-012 首启引导覆盖用户称呼、BYOK 绑定、macOS 权限与三类独立同意；撤回常听立即降为待命——server/app/routers/jarvis.py、web/src/features/jarvis/JarvisPage.tsx、web/src/features/settings/JarvisSection.tsx
- CR-012 唤醒方式与灵敏度可在「设置 · 贾维斯」长期维护；常听按 AEC 能力明确选择全双工或半双工，并显示本机麦克风输入状态——server/domain/listening.py、server/domain/jarvis_voice.py、server/app/routers/jarvis_voice.py、web/src/features/jarvis/voiceClient.ts、web/src/features/settings/JarvisSection.tsx
- CR-015 支持用户输入任意唤醒短语、配置唤醒开场白与生成最多 5 个随机变体；自定义常听由 macOS 本地 Speech 帮助进程匹配，命中后才建立云端语音会话，同句命令不丢失——desktop/native/wake-listener、desktop/src/main.ts、web/src/features/jarvis/jarvisStore.ts、web/src/features/settings/JarvisSection.tsx、server/domain/jarvis_voice.py
- CR-012 桌面包内置 Web、SQLite 基线、sidecar、Open Interpreter `interpreter`/`codex-code-mode-host` 与 openWakeWord 资源，随机回环端口、单次启动令牌与异源拒绝；code-mode host 单独取得 V8/JIT entitlement——server/app/desktop_runtime.py、server/desktop_sidecar.spec、desktop/scripts/prepare-runtime.mjs、desktop/scripts/sign-mac.cjs、desktop/src/main.ts
- 熟人内测使用独立 ad-hoc 构建档并生成手动放行说明；本机 Apple Development 验证档和官网 Developer ID 公证档保持隔离——desktop/scripts/build-local-mac.mjs、desktop/scripts/sign-mac.cjs
- CR-012 本地队列使用 WAL、lease、generation fencing 和幂等 job id，不依赖 Redis/Arq 守护进程——server/app/queue.py、server/app/local_worker.py、tests/test_local_queue.py
- CR-012 数据迁移先 dry-run，后备份、导入、外键/数量/媒体摘要对账与本地文件主密钥迁移——server/scripts/migrate_to_desktop.py、server/app/desktop_runtime.py、tests/test_migrate_to_desktop.py
- 桌面启动令牌只经回环请求头注入，打包关闭 Cookie 加密 fuse；安装包不读写 Keychain——desktop/src/main.ts、desktop/electron-builder.yml、server/app/desktop_runtime.py
- CR-012 发布管线生成 DMG/ZIP、SBOM、许可清单与 Ed25519 更新 manifest，公开包强制 Developer ID 与公证预检——desktop/electron-builder.yml、desktop/scripts/release-mac.mjs、server/scripts/generate_desktop_compliance.py
- CR-013 行动控制台接入登记后的 Jarvis 装甲形象，重组为状态轨、交互舞台与可内部滚动的活动时间线；原始证据默认折叠，截图可放大，深链历史会话不再被正在运行的会话覆盖——web/src/features/jarvis/JarvisPage.tsx、web/src/features/jarvis/jarvis.css、web/src/features/jarvis/jarvisStore.ts
- CR-015 中央形象升级为 Three.js WebGL 3D 舞台，包含视差、景深环、粒子与状态自发光；WebGL 不可用时显示静态图，1280×720 与 960×720 不产生页面级溢出——web/src/features/jarvis/JarvisAvatar3D.tsx、web/src/features/jarvis/jarvis.css、desktop/scripts/probe.mjs
- CR-015 执行子进程默认迁到官方 Codex App Server，保留 NEXUS Kernel、Mission、审批、事件、记忆和工具分层；安装包固定携带 Codex arm64 运行时，不再携带 Open Interpreter standalone——server/domain/jarvis.py、server/domain/open_interpreter_app_server.py、desktop/scripts/prepare-runtime.mjs、desktop/src/main.ts

### 部分完成
- （无）

### 未实现
- （无）

### 超出需求新增
- `server/scripts/jarvis_latency_probe.py`：打断 ack / 急停 / 首 delta / 审批卡四个时间点的 P50 / P95，写 data/logs——M7 观测项提前落地
- `desktop/src/paths.ts`：登录 shell PATH 合并 + 兜底目录（登录项启动的 GUI 进程不继承 shell PATH）——STD-BUILD-001 第 5 条提前落地

## 21 电脑操控（已按 CR-016 退场）

当前状态：不再属于 NEXUS 产品范围。Codex App Server、CuaDriver、agent-browser 门代理、审批窗、急停与通用电脑执行会话均已从运行链路移除。以下条目仅作为历史实施记录，不再表示当前功能。

### ✅ 已完成
- 执行内核：本机 `interpreter 0.0.40 app-server --stdio` 子进程托管（stdio JSON-RPC v2，`initialize → thread/start → turn/start → item/* → turn/completed`）——server/domain/open_interpreter_app_server.py、server/domain/jarvis.py、tests/test_jarvis.py::test_status_reports_rust_app_server
- FR-21-01 会话端点 `POST /jarvis/sessions`、`POST /jarvis/session/ensure`（并发安全唯一会话）、`GET /jarvis/sessions[/{id}]`、`GET /jarvis/status`——server/app/routers/jarvis.py、tests/test_jarvis.py::test_ensure_reuses_one_thread_and_queue_policy
- FR-21-02 SSE 事件流 + 内存 hub——server/domain/jarvis_events.py、server/app/routers/jarvis.py events、tests/test_jarvis.py::test_events_sse_replays_after_cursor
- FR-21-03 `POST .../say {policy}` 202 `{accepted_as, turn_id}`（只有 strict 409）、`POST .../interrupt` 等 ack、慢 ack 走恢复——server/app/routers/jarvis.py、tests/test_jarvis.py（strict_policy_rejects_when_busy / stop_waits_for_interrupt_ack / slow_interrupt_recovers_engine）
- FR-21-10 `build_engine_env` / `build_engine_config` / `render_config_args` / `redacted_config` 纯函数、`FORBIDDEN_METHODS` 一刀切、未知服务端请求拒绝——server/domain/jarvis.py、server/domain/open_interpreter_app_server.py、tests/test_jarvis_config.py（6 条）、tests/test_jarvis.py（forbidden_methods_never_reach_the_engine / unknown_server_request_is_rejected）
- BR-21-05 一半：子进程 env 剥离 `LINGUA_` 密钥、只注入运行时 API key 与 MCP Bearer——tests/test_jarvis_config.py::test_env_strips_lingua_secrets_and_injects_runtime_tokens
- BR-21-06 delta 进 hub、`item/completed` 才落库、崩溃时在飞 item 以 failed 落库——tests/test_jarvis.py（stream_deltas_publish_then_persist / crash_flushes_live_items_as_failed）
- BR-21-07 turn 300s 超时先 interrupt、恢复 ≤ 2 次不无界重试——server/domain/jarvis.py `_watch_turn` / `_recover_handle`、tests/test_jarvis.py（timeout_does_not_replay_previous_speech / slow_interrupt_recovers_engine）
- BR-21-08 急停三处（HUD / 托盘 / ⌘⇧Esc）+ `POST /jarvis/emergency-stop` 终止全部引擎子进程——server/app/routers/jarvis.py、desktop/src/main.ts、web/src/lib/shell.ts emergencyStopJarvis；overlay 窗的建窗 / 穿透函数已有——desktop/src/windows/overlay.ts
- BR-21-09 启动对账只判 `scope=desktop`——server/domain/jarvis.reconcile_on_startup、tests/test_jarvis.py::test_reconcile_marks_desktop_orphans_only
- FR-21-08 能力「电脑操控」与截图清理 cron 04:10——server/app/routers/config.py、server/worker/main.py cron(purge_computer_screenshots)
- BR-21-01 默认范围：`build_engine_config` 产出 `approval_policy="on-request"` + `default_permissions=<profile>` + `permissions.<profile>.*`（受保护路径 deny，不再有 `sandbox_mode`）；`domain/cua_runtime.py` 私有 bounded daemon + `data/jarvis/cua-capabilities.yaml`（v3：工具白名单、App bundle_id 白名单、files 只读三目录、expires 12h / idle 30m）——tests/test_jarvis_config.py、tests/test_cua_runtime.py（2026-09-02 M3）
- BR-21-02 / 03 分级审批：`domain/danger.py`（`DANGER_WORDS` 只增不减 + `DANGER_PROGRAMS` / `WRITE_HTTP_RE` / `PROTECTED_GLOBS` / `classify_command` / `classify_file_change` / `classify_tool_call`）+ `domain/jarvis_gate.py` `ApprovalBroker`：commandExecution / fileChange / permissions / tool.requestUserInput / elicitation 五类分流，低风险 turn 级 `accept`、永不 `acceptForSession`、未知请求 -32601 fail-closed、120s 超时 decline、急停 cancel——tests/test_danger.py、tests/test_jarvis_gate.py（2026-09-02 M3）
- FR-21-04 `POST .../approve {approval_id, decision}`、`POST .../answer {question_id, answers}`、`POST .../gate {level}`、`JarvisPending{approval_id, kind, risk, summary, expires_at, question_id?, questions?}`（审批与提问共用一张卡，密码类问题不落库不念）——server/app/routers/jarvis.py、web/src/components/JarvisApprovalCard.tsx、tests/test_jarvis_gate.py::test_secret_answer_never_lands_in_steps
- FR-21-09 门代理：`domain/jarvis_mcp_gate.py` 用 fastmcp `FastMCPProxy` 把 cua-driver（私有 socket）与 agent-browser（`mcp --tools core` + action policy）挂成 `/mcp/desktop`、`/mcp/browser`（回环 + Bearer，`mount_gated_mcp`），`ToolGate` 中间件按 `classify_tool_call` 过同一个席位、剪贴板读取一律拒、快照 ref → 标签缓存让审批卡写得出「点的是 Submit order」；引擎配置只剩 `mcp_servers.{lingua,desktop,browser}.url`——tests/test_jarvis_mcp_gate.py
- BR-21-05 其余：profile deny 受保护路径、门代理拒 `clipboard_read`——同上
- D5-4 截图留存：门代理把快照类工具带回的图落成 `kind=image` 步骤（`screenshot_key`，7 天 cron 清理），`GET /jarvis/sessions/{id}/screenshot?ordinal=` 取图——server/domain/jarvis_mcp_gate.py `_keep_screenshot`、tests/test_jarvis_mcp_gate.py::test_snapshot_images_land_as_image_steps
- FR-21-11 `GET /jarvis/sessions/{id}/metrics`（在飞与上一轮打点：first_event / first_message_delta / interrupt_ack / turn_total）、`ComputerStep.duration_ms` 已填——tests/test_jarvis.py::test_metrics_endpoint_keeps_last_turn_after_idle
- BR-21-08 overlay 页 `/jarvis/overlay`、急停后 `cua_runtime.revoke_all()` 与 `{ok, stopped, mode, mode_changed}` 响应——server/app/routers/jarvis.py、web/src/features/jarvis/JarvisOverlayPage.tsx
- FR-21-05 / 06 / 07 已废条目的代码已删（2026-09-02 M7）：`domain/computer_use.py`、`routers/computer.py`、`tests/test_computer_use.py`、`web/src/features/computer/*`、`server/engine_runner/`、`data/engines/open-interpreter`（AGPL venv）；`detached_session` / `purge_old_screenshots` / `ACTIVE_STATUSES` / `step_view` 收在 `domain/computer_steps.py`，审批语义上提到 tests/test_jarvis_gate.py
- CR-014 本地开发入口统一为英文名称，默认严格启动 PostgreSQL / Redis、API、Arq worker、Web 与源码 Electron；安装包需显式 `--installed`，失败回收已启动进程，退场的执行引擎不再参与启动，桌面产物未变更时跳过重编——start-nexus.sh、tools/launch_nexus.py、server/tests/test_launch_nexus.py

- BR-21-10 / 11 审批前移（CR-011 D-6）：`danger.classify_utterance` + `spoken_risk` 在用户说完话那一刻判危险意图，`submit()` 起后台任务等裁决并回 `accepted_as="intent"`（不在请求里 await），`ApprovalBroker.confirm` 认 `handle.preapproved` 覆盖同一轮第一次同类请求——server/domain/danger.py、server/domain/jarvis.py、tests/test_jarvis_gate.py（意图门 4 条）（2026-09-03 M87）
- CR-012 意图预授权绑定会话、轮次、动作类别、目标、参数约束和过期时间，只能消费一次——server/domain/jarvis.py、server/domain/jarvis_gate.py
- CR-012 多步、外部写入、待审批与跨进程请求提升为 Mission，关联 StudioFlow/ComputerSession 并按 checkpoint 恢复——server/domain/jarvis_missions.py、server/domain/jarvis.py、server/domain/studio_flows.py
- CR-012 工具合同统一 capability、schema、风险、数据范围、幂等、超时和证据，固定业务 API 到视觉 CUA 的五级顺序——server/domain/jarvis_capabilities.py、tests/test_jarvis_capabilities.py
- CR-012 CuaDriver 作为最后一级视觉/原生执行器，保留上游签名，下载后校验 SHA-256、bundle id、Team ID 与 Gatekeeper——desktop/src/main.ts、server/domain/cua_runtime.py
- CR-012 执行期语音隔离：PCM 抖动缓冲与 underrun 重缓冲、大 JSON 帧线程解析、命令输出不在语音 WS 重复传输——web/src/features/talk/realtimeAudio.ts、server/domain/open_interpreter_app_server.py、server/domain/jarvis_voice.py

### 🔶 部分完成
- agent-browser Authentication Vault（网页侧凭据填充）未接，浏览器登录态仍靠已登录的 Chromium——CR-010 待办
- （无核心链路缺口；Authentication Vault 是可选账号填充设施）

### ❌ 未实现
- （无；截图端点与 daemon 检测已落）

### ➕ 超出需求新增
- `server/tests/fake_app_server.py` 按 `turn/start` 文本选剧本的替身（stream / danger / unsteerable / slow / crash / unknown-request 等）——测试设施，需求里没有
- `desktop/src/windows/approval.ts` 独立审批弹窗（460×360，`present` / `dismiss`）——CR-010 D-6，需求侧已并入 FR-20-13
- `desktop/scripts/probe-aec.mjs` 回声探针（四配置 × N 轮，判 ALWAYS-ON 全双工还是半双工带抢话）——CR-010 P0.5，手动跑

## 22 扩展与例程（2026-09-02，按 CR-010 目标口径）

完成 4 · 部分 0 · 缺失 2 · 新增 0

### 已完成
- BR-22-01 ~ 09 全部：九类贡献点与四类权限字典、贡献数从登记处取、权限门、坏 manifest 列出、MCP 工具集只来自就绪扩展且惰性连接、例程 upsert 与人改优先、到点判定（基准加一秒）、失败记产出、测试只列工具——server/domain/extensions.py、server/domain/routines.py、tests/test_extensions.py（10 条）
- FR-22-01 ~ 08 全部：`/extensions*`、`/routines*`、manifest 模型、PydanticAI `MCPToolset` 挂进助理代理、`routine` 表 + worker 每分钟 `run_due_routines`（早报改走这里）、扩展页两栏、导航与 ⌘K、示例扩展——server/app/routers/extensions.py、server/domain/assistant.run_turn(toolsets=)、server/worker/tasks.py、web/src/features/extensions/ExtensionsPage.tsx、data/extensions/example/manifest.yaml

- CR-016 删除「贾维斯」内置清单、页面与 `/mcp/desktop`、`/mcp/browser` 专属门代理；通用工作台 `/mcp` 和扩展 MCP 装载机制保留。

### 部分完成
- （无）

### 未实现
- 代码包扩展、画布节点 / 设置项 / 数据表的装载点（需求 §3 刻意不做，等第一个真实需求）
- 扩展 manifest 仍没有一个真实外部 MCP 贡献（示例 `mcp:` 段保持注释）。

### 超出需求新增
- 无
