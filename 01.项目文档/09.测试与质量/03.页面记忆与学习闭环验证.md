# 页面记忆与学习闭环验证

日期：2026-09-04。范围：CR-021。结论：代码与目标自动测试已有证据，完整功能、真机媒体与视觉验收尚未完成，不具备整轮验收结论。

## 自动检查

| 检查 | 结果 | 证明范围 |
| --- | --- | --- |
| 后端语法、讲义、批注、对话、记录、快照和迁移目标测试 | 222 项通过，34.07 秒 | 新旧接口、提交去重、首次判定保留、缓存读取、用户隔离、迁移 |
| 视频发现、媒体响应、视频元数据目标测试 | 48 项通过，7.78 秒 | 元数据、字幕、媒体响应及旧链路兼容 |
| 新工作位置／学习接口与迁移测试 | 17 项通过，2.81 秒，包含在上列目标套件 | 非法地址、敏感草稿拒绝、重复收藏、65 条发现分页、评分及缓存隔离 |
| 前端全量测试 | 1,508 项通过，6.50 秒 | 现有自动用例及新增工作位置测试，不代表浏览器交互验收 |
| 工作位置追加回归 | 5 项通过，包含新增超过 500 条快照分页读取 | 旧草稿不会因分页上限丢失；包含在后续前端全量用例中 |
| TypeScript、UI 静态检查 | 通过 | CSS 无碰撞、无遗漏原生 select；滚动扫描 272 个容器，130 条跨组件祖先链静态不可判定 |
| 前端生产构建 | 通过，12.05 秒 | 保留既有 Harper fs 外置、混合导入和大 chunk 提示；未调高阈值隐藏告警 |
| 桌面编译和测试 | 14 项通过 | 编译、代理规则及新 YouTube URL／资源白名单，不包含实际网页播放 |
| Ruff 目标范围 | 通过 | 语法、概念、视频、快照路由和新增测试 |
| 治理 L1 | PASS，warn／standard | 目录、追踪和规范登记结构，不代表业务和真实运行验收 |

一次目标测试命令引用了不存在的 `test_grammar_concepts.py`，该次未执行任何测试；随后改用仓库实际文件重跑，以表内结果为准。

## SQLite 与运行环境

- 开发数据库升级前使用 SQLite 在线备份并执行完整性检查，备份保留在本地数据目录，不纳入版本控制。
- `f2d3e4f5a6b7` 在测试库完成升级、回退、再次升级；旧视频记录保留。
- 实际开发库完成升级；一键启动后的 API 健康检查通过。该事实只证明迁移和进程启动，不证明页面验收。
- 不在真实开发库执行降级；三张新表包含新产生记录，回退前必须另行备份。

## 尚未验证

Mac 锁屏导致计算机使用工具不可用。没有通过其他浏览器或直接连接远程调试绕过该限制，也没有把旧截图作为本轮验证。

- 全菜单进入子页、切出、返回、重启，包含前进后退、删除对象、保存失败。
- 实时会话真实出声、切出后无麦克风上行、返回显式继续、超时和晚到创建结果。
- YouTube 实际加载和播放、代理开关、离开暂停、五分钟释放、浮层遮挡与不同缩放。
- 1280／1440／1920 和深浅主题，长句、记录规模、辅助面板与图形加载定位。
- 词卡、音色、记录的逐层 Esc、焦点、滚动和音频互斥。
- 首开／返回的冷、热基线与多次切换后的内存、播放器及渲染循环资源占用。

CR-021 覆盖表列有尚未实现的细项。结构治理审计、编译和自动测试均不能将这些细项或上述真机检查变成通过。
