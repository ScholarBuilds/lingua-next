# 软件英语 Markdown 讲义验证

日期：2026-09-09。

## 当前内容状态

- 原先 62 篇脚本草稿已归档到本机 `_归档-旧版/脚本草稿-2026-09-09/`，不会显示在阅读器中。
- 新版改为逐篇人工写作；已完成大纲、“读懂系统设置”四篇、“网络与设备连接”八篇、“桌面显示与外观”七篇、“输入声音与快捷操作”五篇、“通知专注与时间管理”九篇、“通用与系统维护”十一篇、“隐私与安全”八篇、“辅助功能”八篇、“账户外设与共享”五篇，以及“表达索引与综合练习”四篇。六十九篇正文均为 8,000–15,000 中文字符，目录不使用图号、文件名或“某某截图”。
- 大纲中的 69 个正文链接已经改为手写讲义的实际文件名，并在发布目录中逐一验证存在；运行时大纲与 Obsidian 源目录保持一致。
- 456 张本机原图均有且仅有一处 Markdown 引用；图像旁保留稳定锚点，个人数据只存在于受控本机原图中，不进入正文、搜索索引、词汇表或练习。
- 原图和来源映射仍保留在本机。个人邮箱、IP、MAC、网络名称和设备编号不写入新版正文、搜索索引、词汇表或练习题。
- 已完成文章使用标准相对图片路径，NEXUS 继续通过受控本机图片接口读取原图。

## 自动检查

- `server/.venv/bin/python -m pytest -q tests/test_grammar_docs.py tests/test_grammar_annotations.py tests/test_workspace_learning.py`：79 项通过。覆盖软件库元数据、软件范围文档树、安全图片读取、MIME、点号目录、旧来源解析及共用讲义状态能力。
- `npm run check:ci`：135 个测试文件、1,655 项测试通过；TypeScript、CSS 类名、滚动链路与生产构建全部通过。覆盖五个 Tab、软件入口先显示选择页、带软件参数才进入阅读器。
- `govern-project audit --level 1`：通过 standard 档位完整性审计。

## 真实页面验收

- 旧 `/software-english` 实际跳转至 `/grammar?tab=software`，旧独立侧栏入口不再出现。
- 软件库卡片继续显示 macOS、27.0 Beta（26A5425a）、2026-09-09 和 456 张截图；教程篇数随人工重写进度更新。
- 点击卡片进入三栏阅读器；新版目录只显示已经人工完成的功能主题，正文使用 Markdown、Callout、Mermaid、界面实例、表达表和自测，右侧保留大纲、笔记、本节练习。
- 点击 Wi-Fi 截图成功打开现有 Overlay；按 Esc 只关闭截图层，焦点恢复到原截图按钮。
- 再次点击“软件英语”实际返回软件库；旧 Wi-Fi 深链实际定位到 `capture-wifi` 截图章节。
- 截图页中的表达锚点保留在标准 Markdown 表格单元格内；实际页面可访问性树识别出完整表格行，不再把裸 HTML 行散落在表格外。
- 390 像素窄屏使用单列软件卡片与收起导航；五个讲义 Tab 可横向滚动，不裁掉“软件英语”。
- 目录展开状态键已改为 `collection + software_id`，不会污染其他讲义集合。

## 边界

- 原图按用户锁定要求保持原始像素且不遮挡，仅允许本机桌面接口读取；个人数据不进入正文、搜索、单词表和统计。
- 27 个受限页面和 1 个不适用页面保留原因。没有通过改变系统配置补采。
- 右栏“本节练习”继续复用现有映射与空状态；正文末尾提供可展开答案的自测。
